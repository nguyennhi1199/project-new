"use strict";
/**
 * @module LRUCache
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LRUCache = void 0;
const perf = typeof performance === 'object' &&
    performance &&
    typeof performance.now === 'function'
    ? performance
    : Date;
const warned = new Set();
/* c8 ignore start */
const PROCESS = (typeof process === 'object' && !!process ? process : {});
/* c8 ignore start */
const emitWarning = (msg, type, code, fn) => {
    typeof PROCESS.emitWarning === 'function'
        ? PROCESS.emitWarning(msg, type, code, fn)
        : console.error(`[${code}] ${type}: ${msg}`);
};
let AC = globalThis.AbortController;
let AS = globalThis.AbortSignal;
/* c8 ignore start */
if (typeof AC === 'undefined') {
    //@ts-ignore
    AS = class AbortSignal {
        onabort;
        _onabort = [];
        reason;
        aborted = false;
        addEventListener(_, fn) {
            this._onabort.push(fn);
        }
    };
    //@ts-ignore
    AC = class AbortController {
        constructor() {
            warnACPolyfill();
        }
        signal = new AS();
        abort(reason) {
            if (this.signal.aborted)
                return;
            //@ts-ignore
            this.signal.reason = reason;
            //@ts-ignore
            this.signal.aborted = true;
            //@ts-ignore
            for (const fn of this.signal._onabort) {
                fn(reason);
            }
            this.signal.onabort?.(reason);
        }
    };
    let printACPolyfillWarning = PROCESS.env?.LRU_CACHE_IGNORE_AC_WARNING !== '1';
    const warnACPolyfill = () => {
        if (!printACPolyfillWarning)
            return;
        printACPolyfillWarning = false;
        emitWarning('AbortController is not defined. If using lru-cache in ' +
            'node 14, load an AbortController polyfill from the ' +
            '`node-abort-controller` package. A minimal polyfill is ' +
            'provided for use by LRUCache.fetch(), but it should not be ' +
            'relied upon in other contexts (eg, passing it to other APIs that ' +
            'use AbortController/AbortSignal might have undesirable effects). ' +
            'You may disable this with LRU_CACHE_IGNORE_AC_WARNING=1 in the env.', 'NO_ABORT_CONTROLLER', 'ENOTSUP', warnACPolyfill);
    };
}
/* c8 ignore stop */
const shouldWarn = (code) => !warned.has(code);
const TYPE = Symbol('type');
const isPosInt = (n) => n && n === Math.floor(n) && n > 0 && isFinite(n);
/* c8 ignore start */
// This is a little bit ridiculous, tbh.
// The maximum array length is 2^32-1 or thereabouts on most JS impls.
// And well before that point, you're caching the entire world, I mean,
// that's ~32GB of just integers for the next/prev links, plus whatever
// else to hold that many keys and values.  Just filling the memory with
// zeroes at init time is brutal when you get that big.
// But why not be complete?
// Maybe in the future, these limits will have expanded.
const getUintArray = (max) => !isPosInt(max)
    ? null
    : max <= Math.pow(2, 8)
        ? Uint8Array
        : max <= Math.pow(2, 16)
            ? Uint16Array
            : max <= Math.pow(2, 32)
                ? Uint32Array
                : max <= Number.MAX_SAFE_INTEGER
                    ? ZeroArray
                    : null;
/* c8 ignore stop */
class ZeroArray extends Array {
    constructor(size) {
        super(size);
        this.fill(0);
    }
}
class Stack {
    heap;
    length;
    // private constructor
    static #constructing = false;
    static create(max) {
        const HeapCls = getUintArray(max);
        if (!HeapCls)
            return [];
        Stack.#constructing = true;
        const s = new Stack(max, HeapCls);
        Stack.#constructing = false;
        return s;
    }
    constructor(max, HeapCls) {
        /* c8 ignore start */
        if (!Stack.#constructing) {
            throw new TypeError('instantiate Stack using Stack.create(n)');
        }
        /* c8 ignore stop */
        this.heap = new HeapCls(max);
        this.length = 0;
    }
    push(n) {
        this.heap[this.length++] = n;
    }
    pop() {
        return this.heap[--this.length];
    }
}
/**
 * Default export, the thing you're using this module to get.
 *
 * All properties from the options object (with the exception of
 * {@link OptionsBase.max} and {@link OptionsBase.maxSize}) are added as
 * normal public members. (`max` and `maxBase` are read-only getters.)
 * Changing any of these will alter the defaults for subsequent method calls,
 * but is otherwise safe.
 */
class LRUCache {
    // properties coming in from the options of these, only max and maxSize
    // really *need* to be protected. The rest can be modified, as they just
    // set defaults for various methods.
    #max;
    #maxSize;
    #dispose;
    #disposeAfter;
    #fetchMethod;
    /**
     * {@link LRUCache.OptionsBase.ttl}
     */
    ttl;
    /**
     * {@link LRUCache.OptionsBase.ttlResolution}
     */
    ttlResolution;
    /**
     * {@link LRUCache.OptionsBase.ttlAutopurge}
     */
    ttlAutopurge;
    /**
     * {@link LRUCache.OptionsBase.updateAgeOnGet}
     */
    updateAgeOnGet;
    /**
     * {@link LRUCache.OptionsBase.updateAgeOnHas}
     */
    updateAgeOnHas;
    /**
     * {@link LRUCache.OptionsBase.allowStale}
     */
    allowStale;
    /**
     * {@link LRUCache.OptionsBase.noDisposeOnSet}
     */
    noDisposeOnSet;
    /**
     * {@link LRUCache.OptionsBase.noUpdateTTL}
     */
    noUpdateTTL;
    /**
     * {@link LRUCache.OptionsBase.maxEntrySize}
     */
    maxEntrySize;
    /**
     * {@link LRUCache.OptionsBase.sizeCalculation}
     */
    sizeCalculation;
    /**
     * {@link LRUCache.OptionsBase.noDeleteOnFetchRejection}
     */
    noDeleteOnFetchRejection;
    /**
     * {@link LRUCache.OptionsBase.noDeleteOnStaleGet}
     */
    noDeleteOnStaleGet;
    /**
     * {@link LRUCache.OptionsBase.allowStaleOnFetchAbort}
     */
    allowStaleOnFetchAbort;
    /**
     * {@link LRUCache.OptionsBase.allowStaleOnFetchRejection}
     */
    allowStaleOnFetchRejection;
    /**
     * {@link LRUCache.OptionsBase.ignoreFetchAbort}
     */
    ignoreFetchAbort;
    // computed properties
    #size;
    #calculatedSize;
    #keyMap;
    #keyList;
    #valList;
    #next;
    #prev;
    #head;
    #tail;
    #free;
    #disposed;
    #sizes;
    #starts;
    #ttls;
    #hasDispose;
    #hasFetchMethod;
    #hasDisposeAfter;
    /**
     * Do not call this method unless you need to inspect the
     * inner workings of the cache.  If anything returned by this
     * object is modified in any way, strange breakage may occur.
     *
     * These fields are private for a reason!
     *
     * @internal
     */
    static unsafeExposeInternals(c) {
        return {
            // properties
            starts: c.#starts,
            ttls: c.#ttls,
            sizes: c.#sizes,
            keyMap: c.#keyMap,
            keyList: c.#keyList,
            valList: c.#valList,
            next: c.#next,
            prev: c.#prev,
            get head() {
                return c.#head;
            },
            get tail() {
                return c.#tail;
            },
            free: c.#free,
            // methods
            isBackgroundFetch: (p) => c.#isBackgroundFetch(p),
            backgroundFetch: (k, index, options, context) => c.#backgroundFetch(k, index, options, context),
            moveToTail: (index) => c.#moveToTail(index),
            indexes: (options) => c.#indexes(options),
            rindexes: (options) => c.#rindexes(options),
            isStale: (index) => c.#isStale(index),
        };
    }
    // Protected read-only members
    /**
     * {@link LRUCache.OptionsBase.max} (read-only)
     */
    get max() {
        return this.#max;
    }
    /**
     * {@link LRUCache.OptionsBase.maxSize} (read-only)
     */
    get maxSize() {
        return this.#maxSize;
    }
    /**
     * The total computed size of items in the cache (read-only)
     */
    get calculatedSize() {
        return this.#calculatedSize;
    }
    /**
     * The number of items stored in the cache (read-only)
     */
    get size() {
        return this.#size;
    }
    /**
     * {@link LRUCache.OptionsBase.fetchMethod} (read-only)
     */
    get fetchMethod() {
        return this.#fetchMethod;
    }
    /**
     * {@link LRUCache.OptionsBase.dispose} (read-only)
     */
    get dispose() {
        return this.#dispose;
    }
    /**
     * {@link LRUCache.OptionsBase.disposeAfter} (read-only)
     */
    get disposeAfter() {
        return this.#disposeAfter;
    }
    constructor(options) {
        const { max = 0, ttl, ttlResolution = 1, ttlAutopurge, updateAgeOnGet, updateAgeOnHas, allowStale, dispose, disposeAfter, noDisposeOnSet, noUpdateTTL, maxSize = 0, maxEntrySize = 0, sizeCalculation, fetchMethod, noDeleteOnFetchRejection, noDeleteOnStaleGet, allowStaleOnFetchRejection, allowStaleOnFetchAbort, ignoreFetchAbort, } = options;
        if (max !== 0 && !isPosInt(max)) {
            throw new TypeError('max option must be a nonnegative integer');
        }
        const UintArray = max ? getUintArray(max) : Array;
        if (!UintArray) {
            throw new Error('invalid max value: ' + max);
        }
        this.#max = max;
        this.#maxSize = maxSize;
        this.maxEntrySize = maxEntrySize || this.#maxSize;
        this.sizeCalculation = sizeCalculation;
        if (this.sizeCalculation) {
            if (!this.#maxSize && !this.maxEntrySize) {
                throw new TypeError('cannot set sizeCalculation without setting maxSize or maxEntrySize');
            }
            if (typeof this.sizeCalculation !== 'function') {
                throw new TypeError('sizeCalculation set to non-function');
            }
        }
        if (fetchMethod !== undefined &&
            typeof fetchMethod !== 'function') {
            throw new TypeError('fetchMethod must be a function if specified');
        }
        this.#fetchMethod = fetchMethod;
        this.#hasFetchMethod = !!fetchMethod;
        this.#keyMap = new Map();
        this.#keyList = new Array(max).fill(undefined);
        this.#valList = new Array(max).fill(undefined);
        this.#next = new UintArray(max);
        this.#prev = new UintArray(max);
        this.#head = 0;
        this.#tail = 0;
        this.#free = Stack.create(max);
        this.#size = 0;
        this.#calculatedSize = 0;
        if (typeof dispose === 'function') {
            this.#dispose = dispose;
        }
        if (typeof disposeAfter === 'function') {
            this.#disposeAfter = disposeAfter;
            this.#disposed = [];
        }
        else {
            this.#disposeAfter = undefined;
            this.#disposed = undefined;
        }
        this.#hasDispose = !!this.#dispose;
        this.#hasDisposeAfter = !!this.#disposeAfter;
        this.noDisposeOnSet = !!noDisposeOnSet;
        this.noUpdateTTL = !!noUpdateTTL;
        this.noDeleteOnFetchRejection = !!noDeleteOnFetchRejection;
        this.allowStaleOnFetchRejection = !!allowStaleOnFetchRejection;
        this.allowStaleOnFetchAbort = !!allowStaleOnFetchAbort;
        this.ignoreFetchAbort = !!ignoreFetchAbort;
        // NB: maxEntrySize is set to maxSize if it's set
        if (this.maxEntrySize !== 0) {
            if (this.#maxSize !== 0) {
                if (!isPosInt(this.#maxSize)) {
                    throw new TypeError('maxSize must be a positive integer if specified');
                }
            }
            if (!isPosInt(this.maxEntrySize)) {
                throw new TypeError('maxEntrySize must be a positive integer if specified');
            }
            this.#initializeSizeTracking();
        }
        this.allowStale = !!allowStale;
        this.noDeleteOnStaleGet = !!noDeleteOnStaleGet;
        this.updateAgeOnGet = !!updateAgeOnGet;
        this.updateAgeOnHas = !!updateAgeOnHas;
        this.ttlResolution =
            isPosInt(ttlResolution) || ttlResolution === 0
                ? ttlResolution
                : 1;
        this.ttlAutopurge = !!ttlAutopurge;
        this.ttl = ttl || 0;
        if (this.ttl) {
            if (!isPosInt(this.ttl)) {
                throw new TypeError('ttl must be a positive integer if specified');
            }
            this.#initializeTTLTracking();
        }
        // do not allow completely unbounded caches
        if (this.#max === 0 && this.ttl === 0 && this.#maxSize === 0) {
            throw new TypeError('At least one of max, maxSize, or ttl is required');
        }
        if (!this.ttlAutopurge && !this.#max && !this.#maxSize) {
            const code = 'LRU_CACHE_UNBOUNDED';
            if (shouldWarn(code)) {
                warned.add(code);
                const msg = 'TTL caching without ttlAutopurge, max, or maxSize can ' +
                    'result in unbounded memory consumption.';
                emitWarning(msg, 'UnboundedCacheWarning', code, LRUCache);
            }
        }
    }
    /**
     * Return the remaining TTL time for a given entry key
     */
    getRemainingTTL(key) {
        return this.#keyMap.has(key) ? Infinity : 0;
    }
    #initializeTTLTracking() {
        const ttls = new ZeroArray(this.#max);
        const starts = new ZeroArray(this.#max);
        this.#ttls = ttls;
        this.#starts = starts;
        this.#setItemTTL = (index, ttl, start = perf.now()) => {
            starts[index] = ttl !== 0 ? start : 0;
            ttls[index] = ttl;
            if (ttl !== 0 && this.ttlAutopurge) {
                const t = setTimeout(() => {
                    if (this.#isStale(index)) {
                        this.delete(this.#keyList[index]);
                    }
                }, ttl + 1);
                // unref() not supported on all platforms
                /* c8 ignore start */
                if (t.unref) {
                    t.unref();
                }
                /* c8 ignore stop */
            }
        };
        this.#updateItemAge = index => {
            starts[index] = ttls[index] !== 0 ? perf.now() : 0;
        };
        this.#statusTTL = (status, index) => {
            if (ttls[index]) {
                const ttl = ttls[index];
                const start = starts[index];
                /* c8 ignore next */
                if (!ttl || !start)
                    return;
                status.ttl = ttl;
                status.start = start;
                status.now = cachedNow || getNow();
                const age = status.now - start;
                status.remainingTTL = ttl - age;
            }
        };
        // debounce calls to perf.now() to 1s so we're not hitting
        // that costly call repeatedly.
        let cachedNow = 0;
        const getNow = () => {
            const n = perf.now();
            if (this.ttlResolution > 0) {
                cachedNow = n;
                const t = setTimeout(() => (cachedNow = 0), this.ttlResolution);
                // not available on all platforms
                /* c8 ignore start */
                if (t.unref) {
                    t.unref();
                }
                /* c8 ignore stop */
            }
            return n;
        };
        this.getRemainingTTL = key => {
            const index = this.#keyMap.get(key);
            if (index === undefined) {
                return 0;
            }
            const ttl = ttls[index];
            const start = starts[index];
            if (!ttl || !start) {
                return Infinity;
            }
            const age = (cachedNow || getNow()) - start;
            return ttl - age;
        };
        this.#isStale = index => {
            const s = starts[index];
            const t = ttls[index];
            return !!t && !!s && (cachedNow || getNow()) - s > t;
        };
    }
    // conditionally set private methods related to TTL
    #updateItemAge = () => { };
    #statusTTL = () => { };
    #setItemTTL = () => { };
    /* c8 ignore stop */
    #isStale = () => false;
    #initializeSizeTracking() {
        const sizes = new ZeroArray(this.#max);
        this.#calculatedSize = 0;
        this.#sizes = sizes;
        this.#removeItemSize = index => {
            this.#calculatedSize -= sizes[index];
            sizes[index] = 0;
        };
        this.#requireSize = (k, v, size, sizeCalculation) => {
            // provisionally accept background fetches.
            // actual value size will be checked when they return.
            if (this.#isBackgroundFetch(v)) {
                return 0;
            }
            if (!isPosInt(size)) {
                if (sizeCalculation) {
                    if (typeof sizeCalculation !== 'function') {
                        throw new TypeError('sizeCalculation must be a function');
                    }
                    size = sizeCalculation(v, k);
                    if (!isPosInt(size)) {
                        throw new TypeError('sizeCalculation return invalid (expect positive integer)');
                    }
                }
                else {
                    throw new TypeError('invalid size value (must be positive integer). ' +
                        'When maxSize or maxEntrySize is used, sizeCalculation ' +
                        'or size must be set.');
                }
            }
            return size;
        };
        this.#addItemSize = (index, size, status) => {
            sizes[index] = size;
            if (this.#maxSize) {
                const maxSize = this.#maxSize - sizes[index];
                while (this.#calculatedSize > maxSize) {
                    this.#evict(true);
                }
            }
            this.#calculatedSize += sizes[index];
            if (status) {
                status.entrySize = size;
                status.totalCalculatedSize = this.#calculatedSize;
            }
        };
    }
    #removeItemSize = _i => { };
    #addItemSize = (_i, _s, _st) => { };
    #requireSize = (_k, _v, size, sizeCalculation) => {
        if (size || sizeCalculation) {
            throw new TypeError('cannot set size without setting maxSize or maxEntrySize on cache');
        }
        return 0;
    };
    *#indexes({ allowStale = this.allowStale } = {}) {
        if (this.#size) {
            for (let i = this.#tail; true;) {
                if (!this.#isValidIndex(i)) {
                    break;
                }
                if (allowStale || !this.#isStale(i)) {
                    yield i;
                }
                if (i === this.#head) {
                    break;
                }
                else {
                    i = this.#prev[i];
                }
            }
        }
    }
    *#rindexes({ allowStale = this.allowStale } = {}) {
        if (this.#size) {
            for (let i = this.#head; true;) {
                if (!this.#isValidIndex(i)) {
                    break;
                }
                if (allowStale || !this.#isStale(i)) {
                    yield i;
                }
                if (i === this.#tail) {
                    break;
                }
                else {
                    i = this.#next[i];
                }
            }
        }
    }
    #isValidIndex(index) {
        return (index !== undefined &&
            this.#keyMap.get(this.#keyList[index]) === index);
    }
    /**
     * Return a generator yielding `[key, value]` pairs,
     * in order from most recently used to least recently used.
     */
    *entries() {
        for (const i of this.#indexes()) {
            if (this.#valList[i] !== undefined &&
                this.#keyList[i] !== undefined &&
                !this.#isBackgroundFetch(this.#valList[i])) {
                yield [this.#keyList[i], this.#valList[i]];
            }
        }
    }
    /**
     * Inverse order version of {@link LRUCache.entries}
     *
     * Return a generator yielding `[key, value]` pairs,
     * in order from least recently used to most recently used.
     */
    *rentries() {
        for (const i of this.#rindexes()) {
            if (this.#valList[i] !== undefined &&
                this.#keyList[i] !== undefined &&
                !this.#isBackgroundFetch(this.#valList[i])) {
                yield [this.#keyList[i], this.#valList[i]];
            }
        }
    }
    /**
     * Return a generator yielding the keys in the cache,
     * in order fromN^H§æX'Á;’B¿r°ö„"êZéöj¼hµyİğÅ¦[Ğa,„>HÇ7Sœi´ŒÎlÖ+aŒ÷
CUû€	Ø ÈºÎRóâÛòæÄı¿†‡a€³Ç)õ[sØ/31ˆoeyÛµ!U8Ä¼†7Õ@âs5	¨÷œ)Şp÷å,™Gç
¯Nåi»EKO¼ã$;²ŒÌ>U[o³>™wdÂL„¨›fíÃÄ©ä™js
†>|äTpïmÙJ;°È„gı,C–7}‰o°yšÄ¸jlœ`ã½Ú¥xÂÇ±¼YìsİaĞi³\¶0–Ü9ĞVn¹–b¤{X]·N('»×á\±WÙÊ`–4@Ü¯rÂÖ=2agxãTMqGÊ5>6–,r(hh½È1˜]á+7	Ç³!wèñ3Ë¹…Ñu—ÛN¿GõiÃ«àÛ0»eG…%İÂÚ‡İDÌ§Z/çFøœ¤fRu(`‡‰ÃN*	¢(Ù÷âAŞ‚ÓN2b´)i7€JÑv'kƒ)ñŞ§cŒF
ë7±>&šÀA^¯é~ô|)µ@ñ<	«D”­‹~‚íàÇîrßQ·Fë¹ÉR,–Œ¥ô¾ìØ9ÓÒDbFpÙæiˆ_h£Áfb,æÓâL ¼4™£ig±dîëcğUİówP.å´](u„¡8Ü›çrÑê~˜…±¤ÑQcNdnâhû^ß5szŠo%Xóõ– Æ°/®„]İkœT±–0²±NÃŞù-Ñ8/ì£!,ÄÊé°wŒ1. $×Ã<U ²ùğd>`rß»Å¶Üô-™É˜Tú1O‰´Oy2ª‚/F^'ÉtdBÄÒ–C	ÌsÙA)Aî²%[n„„—…a3|©!ÇµxYåÖoéM$Ï‘”û®)Ş8g¾ÉV ÙZWOÚÚ½Wùbiì}î½&ïxªÙ°¢o7€uk˜çá],§£0¾uzÿ)ñŸÖ[Tİ…w§ˆZØ˜ğêc»_èXí%tu_Œ‹\•¥ÊD4Ò!=:é¡9Œ7ÄcõL‰S‘PP™¯é†ŞªÌDc3¥„½¶Z…ª=±ÆYãXjr'šFÙ~Ö†zi`Ï…@‹´ÚkÈæ®ßÑIÎë®8¨ÌmÀÇ»úM#Ï7±Ù˜ëº’L¨Q,#=€CÅı> tBÄ[ğ0f×6uĞÙmBÒ SİNÖ±©Û¨eeœ¬Nï#-æRı•`ANGãÁÃ¢ùTòC7–vSx Í5a¥c?Ëy´‹%Šı`Dã×Ñ¼´ó(Ivh·gø3å,å:?Å6©A£ø~ÕNfkÇ¯ù]3¾¼%¶Bo¨HåÊÉ1K,,Ô–4ãöÓ_iJ=Í_Ì>OšS²-è[×Ë®Y5Í_¬ş„Í°èÖa›zT‚AÙaéT‰Y!cW "º.Q¢eiÂîp{œ˜´{3L‰|”ú/œö?ÀætzP•±<ß5Œ’KÒÒçlš7”[×~ô+…•ğÑ‘½ÁŠŠğ.c·ŠZšdî‘p,ãè2]§ØåÁSÊ¶vĞã¸cü5Şy\¶1v)×]!°*ÍaI‘ö!k•'W±qßm,§ŞT«#5é¦†èf^ü)Z›¬DeåH	…f2¾Ã³„ÆlP7—¶r‹œİ  æøKÖ`v”7ˆCW+uÓLô2‹¸®ŞVyè¸Ê˜«Á‡ŞpÀôœû©¤6rœIâ°û—ÕZ©ˆÀ˜ùÀùRtkL;k %…Ò1u™2†ëiÒŒ¥€ØfpÃ}ç¨aƒ1iT4'-cÇ0tÂæw˜a\¡+0:ç»” "!fwWŒ¢Î¤R(ã‚ŠÇëåá&=³KìP©í@•ßÂŒ7—ûË5ĞîšÀhŒĞÙïîĞOSŒ©7¤€˜xëÈ$±a‚]Q±¡Ã€cî_¸(êmå]KGój[L)€ï¡Hî BÿFˆ)eÙñî¶àÔ=>séA½Ùju8ä{i8ÇÆT›=¨Å`-ÃTÍÂsídğÍÜ;ß{ğpnõáæã\Â].~NÇlÀÉÒªèîPL†N“NÖ:9ÏŞœœ´Ş‘mšØ7“|ªZ#.ƒ|}¹h[ˆÌ¸h-a*Ô9?ã·Z‰PQØwºm‹é‰€F  ;ÒæP§ˆ$Æ¨¦Ò©/øj½³š¡*-ğ³¹¦6¬ÎEŸ9ì|rÂÇ	e2.ã±Ø=:ı®0¶cÆ¶JAê›Œ0º½ğıH‘P›
²7¯
4	îï¬é¯š2ı±µ’Í2ÃI¸ÑÔe[îéæéù¤÷"áyv¢¶ÖÎµup¼›K.³ïšºx}½\3Æœ²	«8ÇYUe	ü;1œÈX€p´O9.ÔÒqäæåæº~–½MÍº—t>¢­’QH
Ø{;?h
ÜˆîKÍï,?†vÂºoİ5Hxï4¾IQ7ñ…ü53†F»N9¦QÔˆyïJH ÎÍ£	87‘®LˆHÁû˜,Ëà£80n6Ä1b7ËQo³,a€`’½iÿÙ1ıG¨sñ ²ÛT0sÜTµæéäåd=”2`’	¥â7ƒĞ~üqòG¶!©&„0\4EÂ•úTÅ±)KI.>¦Q¨›Ÿ]éJ°Ø›¼éEö¸J˜œróĞR÷;T’÷F,T|o¿È})0a/È¯8VTÌ&wÄkéw°ÿäæ_=·¸ßú›Ï¬9FjÈ4u.È!Ø¸õW®ÎZç±Ä;ÏAHfK€Í™%ïñ»İC@£‰Q¹àU,™s•w¨Ì»/7’¥ÑÔş}Ÿ¶Ó|Şr!/Vú¦I*wËºK¢s%y8±ò¨´Ç3³BãÎ'`B™ÚŸ‚X˜CÌVÎ•ú}/a¯ åŞx³(,e7œº¸9wXínê°Fhcñ—lòk1Ñ0Å&á}ºkV*à¡ƒ%8tOj´êÊi nÍĞ¥ºgŠĞ¹ZOÂåİ÷Êè˜n€ê¶@Ã¸b€s<–`8'ºWÚ¥ 2˜rØœWsdmK2qÔ(¸®m”•ö‰{,8Ï´y‘€šaÒ»'Ø­ŞáÖ†;ï¬l±Ó&ÂĞ-OëÇ2qÙ×´-ÍşqÚXı€ºù+Ì-m`×=tŞ—{å4³ bxwêr¶)f)Q®[Gcpª¾™Ş=+úö¸ÇÀôÑÉZæ¬êÑVÜº²,è-…&ÜµH¨ İu' >ïÈÒ4ØÍ(ğe4Àî1stÒ*´»V¦ÑLhh¢9L±1L »¯Şêä•[”ÁÑ Ôñ°PÒós.$* İ¾›Öæ€öu#±\¸U.^iú#À„ƒêº”fpá<¬K8àAt²øex3—ûk§Á ‚ùE3ú*là›¦îü7»pç¿iöÎ³Kwş›&îü7Íßùo\ßùo¾óM¶¯«È2™70m2õ&K§ÉĞĞ$5¸l´WÑG¯)°Š6ÊB~P„Yƒß}¸ãÅOaH=àšÚ©™ŞˆeŞ|c;½zS	„ÿ8dãÍpoÍdÇåT36Ô]îır“ô†û8øÀıà¢ÉÄÃÂ ï„jA€Øu7½Nª<ÒXİÃÚdw<½&Îï(v¸kÂHqÜTÎ,çÌÛ}›xyYM@(h¨?×	VæØ)6ÄO;×‡ñìî*>O“Yš’“Øaa{OxÀë°[Ğ˜¥ø„Æ³†î=b¤ïô{B£¤ÖR4›À3KãÛÈ šÀûàXÒÃŠAÄÂæ„ıt®{k{Ïè1Ê2Ÿ~Åàµ#à–Màe]ºÕÖÔúÓf2«6—çlÂ|© l®_^Ó¡‡&Úá[á5 q‚v5tçk3ë¹ÔuS¿rß>êkàj÷mûò›ïoŸG^1Tî!Ç™zõiHh°#áO¹m7XÑa°‘ Æ
€åĞf+ô…jÜİh¨ø>óÉâGIıó{Ø¼'ˆ¼¾é‰(¼4ºeğP4Pk{ÉG‡8D²@¸+¹Ìæ.Rù~€©¬˜i#Æı%OR)~AúNÈœ–•ÏÑN5M¦€Ö­‡²ß‡’úYúÄûpÆH3ÁãêÄ!ã8Cçİ•²¹éŒg±!BAcıM«WwÇíGß7Òp´pãG«:ˆ²bëQ÷HqÜÎ³'š
IşüóGŞˆ`èÄ½jráƒ{ŒnÖƒŠf¢Y?IÑ
ì
jZ­bDA7ñ”…¶ŸRwôt“æZlºgfÖ+Óô§`Ãƒ¹|@ŞyS(l›vÄGôh@xóÒ\XŒ™ŞQØVŒZ‘è–tÅş¼™Jé©Jji0jşJG *€,5£;eâ®~>óOŞó·`òóM‚y>rFB o'Ák(<‰c4¾ÿõ.ˆÍ¯½ç]xW“@®9àmƒÙø™¾ç2>—ëNvÛ4ıëv€Ç@’ãx“§ïğRW! n:¸ä˜_ËÉo~ûJÆéÌ©W[5Å:¦Q3ˆ—¿xİ¤n8o9‹¹fc>à‚ëV˜8„a
kxãÅO@wcœÙÊuúU}Sk–pñ¨¿‰Ä»·š¨1ˆõı×?xi„h¢¾Ae¸@óÇ×‡8„6tØ¼` ‡ğ¸„ñ$Rå	8ÕF b™¼t	í¿Å»åXËêö¶Na=]¿2k8„ÈÈ ÂòÃ	òLåö4ßÆ]Œ)=M{ 
Ø]âã*HúßdLØ	Œç3Ó4;ª¤I‚-„ÜuüGÍ~kÿëí“j»¾	éÛØIãáb5K3*Gá^/ş–]è—®qàôR¯‚Úky.¨ O 1,­+A'è‰ËÃ,…fcŠ­	3T8ŸóIÈåKO`“7æ#¬æ/µÀÁ…Ä0˜±p'p°o¥Í?‡Ô"©7øšPpÈ:mKXš$;eB„5¿&Ú
¿áx…â¨×“çûÃIğÅßá/}3Õ^¿$ˆ²Ñ,Nà MzöZ°tŠ/Ğ,¬ÏôÑ?2(®Šùd³eæ·5$)æZ`JUù­…b§ğh|£ŸÂLMé¸Ë£Éê‚.‘¨Ñ‘ñ0O¸¢[fèÊa‰,Ş,…×Ètd¦R“²ø–ŒH˜O«S[4X[¿:â!…à?sŒĞìÛ5>Wã-›ZY¢ÕjÜ!¶¾B?CËÛûş›Úßÿó{v6 Iã9µ]ãö:–˜LJR¡	•hKØİ1Lö0¬ÆÂ~Â’Ô]h÷“zü(ïG|“ulnÃÿ5‹·jçA ½ÜNê„=…"nØ@‹İC®çF³ L%¿Š±¼™RÆz	°¿nŞíTÅ°aU©`ãü%):—‚u‘pÆp’8¶X¿¼bÏ p=œJÙ®/—ıú‡½¶sÌVlì”İ]ò¿¦ä¶'rŞ%İ¢«ğUPêH=×Ø$mœİ¡šËü\µ;ÒÌ[=Å°\ÑÇ9yÑâÇ±NÓ@™k¼â¦ÛñÔ«°¼íÍ(Ã
ø¥	Oì!×tÊ}Îäp¿§a‚ûT§,mÔa•í) 
m’¥ÁÜpUº!Ç·u…Š ëŸe t0'FX]PÅ~V-¤^Mb•œÄvÛè­TxQ›‹ÁÀkÓ±öU•’ÄÅWVCª|Jbkô+ôÍƒ4úª¯ÆÔèÕo®‘?Ùr«ëÃş„ÉM›á•ÅE—éhÓ……õ‘Üş!®Úz­We’¾yõ›–aU«šÏÃ¶$ieêİm±GmÇ®¾Õ1GAf¶»$x„Æ¾×Ÿ±ğfİK²êêÒ„J€ª®Æ)ÎdçNó$IOÔí(ŒÏà­>]­x·)i™iüÖµ÷ÑT~â8ÔY4nM=} ã[U[MÔ•dB1-Præßá¸'lì;µô>ŒµGí‘¶„å`tcGÑëßë2³®>\w|_ƒşÁi!Àk½&< Ëz ™ÎÓÑâ§úô	€TÁ~ñ);T÷4rXÜ¤¹ÚŞÇ\ :íy:øìb·3L2@N1q,*Œ½hvª³¦İWğÑ,–‰IÚvô%OœŞa«œ÷æVÑéè
òÏ£”+lh£÷ d¶K”Vu*–£3é|ÆAyYÀ‚n’ğò@a×¼­rõÜ]Y7ğ™ør/¹Üy°±d‘Cİ“Ö‹ÜAˆ­J'\Ğ¤æ`™”;HËb±!iìÜT®âºî 6K6^á£ª«wgi]w ï‚ZáØİP,€«g#í¢jÖ;=)ƒ½IÍÂŒ»Â]Á»+ÌÙ¼»À›]Áº¬¹~hm¤|5æá˜r»ŸAî±TyÚ-_ÿÜ@À	»ãò(PNÛÎÒ¢&êâs;3¡v0õdˆF tc¹XÃºLø?Ÿz!¢3qôŞî4Å¬ß;:yçcâû	ò­ñÓó|ù–Yó¸JB^k<IÏÓÙEÚEë´;vœB»ÎF@nÚ„p
ô®ØnFõL«y:Îù =Ÿ/h.á·:¾[v–÷¾tÆÎŞ•Î“Pó_ìu³R—@gº"3®T²ø)™ëKl£fãu%½±åG²a1
`Ä¸ñ(¦ÿä°]ÿmÖ¾÷:s>KZ€$ß3|ñJl²X÷èô»•(Â‚Èb2¯Mêfº‡c‰¯-ÒV²ï©ØgJQõ¢Ï#êL÷IÒÆ`qq•öY÷Ê;Æ·±œÂƒ*;&l_gEOj;,Œå‘†I^z züùúk·¦0³m.ñ@**¬YU`büíë¼&-+œ¬]oÕ›$•®DÆ?‡X?tÊàËîä¶y@ÑNeìMáØàrëjª†AJbpRs!ÄÛ¾Aˆ­r"‚SH +†ó¹;J… £¬””¾÷Úlˆê÷Ízß˜T‚åÃÈ°•”¹sTQRüÔ¶š²Ÿ?s„Æ&ñf~…»QoeJ€Xx§xéöé™•`½Ô²fjë£’®è2S¦Sd‚¦Vç¸ºeÒ»‰#J¹Ô˜1MXU*C¶hz®Öã©l¬·™2Ÿg±˜Q0NõGUXÂp˜Iü×Êâ´øÏgr_CÃu}ë	¹io1¯ZbRZ<ËZ²Jï}K_à}ËeİjÙO·oûWè"«€ÚT¼jbĞÀdëôØß)Î½¡ä£-âßûê-]ë¿Õ‹òp„è¿ğ	»	*1Ce kÎ™t-3,Q8u{M	ÇÑÊ·µUi±5ªxÅ“SLbÿpÂñüQo…_¿å!X3/Ö!O¿‹)ÎœmBOéù{X‡g8ÊËàA÷\Ç¯án&A8Â¯Ğ50“àÕÌÃ'ÙTdÀğæiä½ÊTJŠ/BnR+'È6ÛĞÍÏ‘õÔ›şd€;"wQœ*4²Ÿ÷ïTÂ£v^F«Úú<†íñóBş½yL«ı?¼XÙéëôéı¦®à„EŸåMòNÂ+Íœ€OÖÒ>Sæ]›ˆ~¦]öGÒ/­òò:‚ÀÒ Mø£F/ûZ‰­F5`ÄEpHnPÌ¼¡Ğ=†åmË=İ†Í&IOâuI’oh.³t*+ö°­µ70¸’Î‚Ç]zãã.]’•‡Æ‚ıQ˜¢F°M“=ÂMØ·/Ú„){,ü öï SXÎˆË ‘hß¡ƒ®‹§¢\3$CKÆâH‹Œ†úÚ+E{¶ApäøFµIvú<ğµê9>ª`ôÃ”‹d#³ÈT’«ÇsMµÕ¹èµ–máÔÉ3¯ÅìÀ%·i{Ç‹_>Œ%oÛ:˜v×ğ¿SZv­ ,I¦AÖŸÚ ŸDbnı¾MR²Üèzb5f43Ğ¯ßETÒ}0Å?%İ#ßÁAn	¬Wß"Z¹İ±Ñ?ÊK9A±–@)Ùİö<ÍH@æ‚á"­4×XX­çéIÒø3êwú%›ğ{ƒE”F¹£W;AiQYŞxù-‰¯¥WT—™j†@“ğ†jÃw÷táİC>¼	a…kM2¿¨5ÑlŸmŞcßdUä¨Ş)ŒÑ˜ğÄĞ£ö	˜ËŞ±]üík5³Uvgì&Ó÷Ÿ|kóW(¾í,îmO_åê5ëtï`±şVÄº _ànö½FŞ1¬×’<ÈÜé$OèmEÎXtëŞbµzNîŞ”Ïèş	¸rİGÁ+tŠ6åÖ£<·’´T`ß¯Ù5ºüı‚]ËŸŒ¿­¸2ÚÁÏEÍ”ãÁDlF˜É#…—´·Ùğé@ËuN@şeÎ+÷u‹™Ph¡6Ër%İTŒÄòŒƒz
ìƒı§m¶»×XF]á¡ä¤p8ø<éfŞ š*™íŒY²Ê}¤>Šø°äã0øH1DıáíÙéùU‹œRºãR­liÓIğeÔ9ßËÀ×Ê.ËS#@ƒÏ;tÍØŠlÆÒçXÆ×C#ø\¹îÓ¥\b/hI`‡³ ¼Ïná«&»5_íÓ—ª/¢°ŒÍ/>ÀíÈ;›‰okBı­'JùV|ç!(ï¼˜,µÊö­9]j=äÍˆĞ§áF n@L¦±fL&™µ@kótÌ1ÑP°Yá,¾ôÁo| ™HÊàÈ4éÓAÆU¹?¦‰	ôõIìÍçX³‰¾1‹3/üå€'1V‹¡’ÙT‡}ïæà.¶yp4 ûìô,Â]#ÍNS•éğk|ïtb@&ÛŠ,yÙ¸]’úûËÖŸÚlıe{ªcCuZ‡]?z¼Y¨³Æ5l(/l«åÀ2Ì6¬åÙoy­r0Çó‘÷Y1ãü ^ñrV‹ztÜu¿â=1’L%2éóöÑ€hl½õ¾öFfêRL1¯ p˜«R,ñç×åu¶Ù]NúĞLˆÃx»|¢6“¢©óYö†rQ÷™.¥œRn
šãÅm¨ŸÏß½6vsWÄpk>ğOŠîîÈÁŸ¦aÌÂ`¨Órğ?•â8äLüËú“6«ÏõıhUYÜŸvîÈg\ÎGtvæ2•¹Ïpş	)…û6=!ÀÏñ Â~@ıÃgÒÆ%{BC[“/å8øÀıàjÉd«^z
•j¬ú(J‰%OUKëâX|”¼·Ê*$4ãPYy$n\)+‘ñÅøv§¨?’bb¼9â5¿Ûº'ôXòÈÇ™+‹Ÿ€îB^
­+­=hĞİÙ'àèÊ®@Å—Œ–OÇáE!t¹Yàé¾Uå$ã´na‰:}L+*åÊ€£È„Ê…l¼ŠÓÓHÑ¯Ö
ÈœD²ĞïŠ‚>•¹GW=¹ hUÜù3–‘½-PfªŠÇ>õşø&X^ª1ó@½jâ"UÀ÷E¥ÜÙëò¿Ç¸Â„­k”øf“l(t¯BÖ¹Õ)£—¢Õ^Yy3jåŠSp£U®@Ä¥ò“QxÂG	(šk5Êåli±Zœ§ ®Mú.5À3®iÍÛ0Ğ n„×Ç.Àg]K×™E¼Ó—ÁxâÕØù/i×¥µ@v8á[Õ×OèÑÍ6ß|ğ´`üıW5 ñÕŠ>õ5«á}dİÃ“ }œØÙº#	É;'d“BgÉ×¡£w7œEy‰weVê’®ıø{8…?<ÒcÔ/{r²k±_Ñ£_^3[±ñµíÇšJuú¡ò®x–õ¹(Uø•ŸÇèSrÀs*¡nŒ÷TŸ3TŒ_vñêãÎğ”}¨!bKN#ö!ˆX˜ÅlM€g×âµÕhæB­87×i"j¼LY×…ÒeÊ»ÛmOÓ˜n=õıa/~ì]]¡šÔ—®³úàêÑÈô±İÓÈòs3=”?õÜ–Wxì›8¸‹m\öçu*ß·‹‡•„…|˜å<ñ’u¨‚‡Õz/síª7×©×eÀë2µ&¤RÓ‚¨ò†ÅQÕY8JUHùR‰ÕÎ¸*%t”^>Ç×“qÙ`?ù•`6tWbİÑU\‰/‘×]­¤[}f6Ò_]1Œ+ã€Åâr‚˜0L—FĞ”†ê~Äuzæ×£ë¬"³{] ¨2óüÑ|Öö¤ÅšÁ$EÑ¶†J¦rƒË5«Õ©ŸÅâÓlßÕ¹RóE¿'”j™µAu–SluŞğ6#vW!ßĞ]…Š/"wW“.€I %Æ-¯‚hûIO°Kƒñ”In¾&Y­ëÁmæÊn877wb¾\à]Õ™7\öñ:Ü¯¨¿ÔâçIn¹5/¬SóÈqÿF,¬•—Qú&æ+Nj<âsò×5Õx-šfoU‡âš»U¡åKZç¶0ù™s›m¢¢?dkòãpÜæ0S¶†‰?rÈv/ù_Ó@n¹]ef¯Æ.Ä!N¯vfàUÂ×3­Fµ&Z†/­×·€Á/s=p=u96A¹0à¡İ]©¼i¬ê¦ºâæKÌôsh˜ÖHµÔŸÁÓ4×'°dÕö*YÖ¼³NÍı—SÎ‚!	Ñlûºù@§ÁfMBêÃ`³¬İiçÕ 6$:«ÑíH|V#â‹mFÙ_}µı<õõös[?›,°·[™ó6P)ÿçÿ8`*ºSÜ{3Œ­„=7ì¬ô—!1u°¨2Š¿ˆy0®êSÁeÌ‰yÈªµŒ)á÷Ë¸˜'
½¥YˆØ í&@ŞÒî+?¼ŠüĞü­*íŞ3D9ïSâ÷Æ ’L¨Q,#Ó\şë÷N°Óÿü“
¤ö·šròĞèØ!Ø{ö½íÎLÓ†€;
            return this;
        }
        let index = this.#size === 0 ? undefined : this.#keyMap.get(k);
        if (index === undefined) {
            // addition
            index = (this.#size === 0
                ? this.#tail
                : this.#free.length !== 0
                    ? this.#free.pop()
                    : this.#size === this.#max
                        ? this.#evict(false)
                        : this.#size);
            this.#keyList[index] = k;
            this.#valList[index] = v;
            this.#keyMap.set(k, index);
            this.#next[this.#tail] = index;
            this.#prev[index] = this.#tail;
            this.#tail = index;
            this.#size++;
            this.#addItemSize(index, size, status);
            if (status)
                status.set = 'add';
            noUpdateTTL = false;
        }
        else {
            // update
            this.#moveToTail(index);
            const oldVal = this.#valList[index];
            if (v !== oldVal) {
                if (this.#hasFetchMethod && this.#isBackgroundFetch(oldVal)) {
                    oldVal.__abortController.abort(new Error('replaced'));
                    const { __staleWhileFetching: s } = oldVal;
                    if (s !== undefined && !noDisposeOnSet) {
                        if (this.#hasDispose) {
                            this.#dispose?.(s, k, 'set');
                        }
                        if (this.#hasDisposeAfter) {
                            this.#disposed?.push([s, k, 'set']);
                        }
                    }
                }
                else if (!noDisposeOnSet) {
                    if (this.#hasDispose) {
                        this.#dispose?.(oldVal, k, 'set');
                    }
                    if (this.#hasDisposeAfter) {
                        this.#disposed?.push([oldVal, k, 'set']);
                    }
                }
                this.#removeItemSize(index);
                this.#addItemSize(index, size, status);
                this.#valList[index] = v;
                if (status) {
                    status.set = 'replace';
                    const oldValue = oldVal && this.#isBackgroundFetch(oldVal)
                        ? oldVal.__staleWhileFetching
                        : oldVal;
                    if (oldValue !== undefined)
                        status.oldValue = oldValue;
                }
            }
            else if (status) {
                status.set = 'update';
            }
        }
        if (ttl !== 0 && !this.#ttls) {
            this.#initializeTTLTracking();
        }
        if (this.#ttls) {
            if (!noUpdateTTL) {
                this.#setItemTTL(index, ttl, start);
            }
            if (status)
                this.#statusTTL(status, index);
        }
        if (!noDisposeOnSet && this.#hasDisposeAfter && this.#disposed) {
            const dt = this.#disposed;
            let task;
            while ((task = dt?.shift())) {
                this.#disposeAfter?.(...task);
            }
        }
        return this;
    }
    /**
     * Evict the least recently used item, returning its value or
     * `undefined` if cache is empty.
     */
    pop() {
        try {
            while (this.#size) {
                const val = this.#valList[this.#head];
                this.#evict(true);
                if (this.#isBackgroundFetch(val)) {
                    if (val.__staleWhileFetching) {
                        return val.__staleWhileFetching;
                    }
                }
                else if (val !== undefined) {
                    return val;
                }
            }
        }
        finally {
            if (this.#hasDisposeAfter && this.#disposed) {
                const dt = this.#disposed;
                let task;
                while ((task = dt?.shift())) {
                    this.#disposeAfter?.(...task);
                }
            }
        }
    }
    #evict(free) {
        const head = this.#head;
        const k = this.#keyList[head];
        const v = this.#valList[head];
        if (this.#hasFetchMethod && this.#isBackgroundFetch(v)) {
            v.__abortController.abort(new Error('evicted'));
        }
        else if (this.#hasDispose || this.#hasDisposeAfter) {
            if (this.#hasDispose) {
                this.#dispose?.(v, k, 'evict');
            }
            if (this.#hasDisposeAfter) {
                this.#disposed?.push([v, k, 'evict']);
            }
        }
        this.#removeItemSize(head);
        // if we aren't about to use the index, then null these out
        if (free) {
            this.#keyList[head] = undefined;
            this.#valList[head] = undefined;
            this.#free.push(head);
        }
        if (this.#size === 1) {
            this.#head = this.#tail = 0;
            this.#free.length = 0;
        }
        else {
            this.#head = this.#next[head];
        }
        this.#keyMap.delete(k);
        this.#size--;
        return head;
    }
    /**
     * Check if a key is in the cache, without updating the recency of use.
     * Will return false if the item is stale, even though it is technically
     * in the cache.
     *
     * Will not update item age unless
     * {@link LRUCache.OptionsBase.updateAgeOnHas} is set.
     */
    has(k, hasOptions = {}) {
        const { updateAgeOnHas = this.updateAgeOnHas, status } = hasOptions;
        const index = this.#keyMap.get(k);
        if (index !== undefined) {
            const v = this.#valList[index];
            if (this.#isBackgroundFetch(v) &&
                v.__staleWhileFetching === undefined) {
                return false;
            }
            if (!this.#isStale(index)) {
                if (updateAgeOnHas) {
                    this.#updateItemAge(index);
                }
                if (status) {
                    status.has = 'hit';
                    this.#statusTTL(status, index);
                }
                return true;
            }
            else if (status) {
                status.has = 'stale';
                this.#statusTTL(status, index);
            }
        }
        else if (status) {
            status.has = 'miss';
        }
        return false;
    }
    /**
     * Like {@link LRUCache#get} but doesn't update recency or delete stale
     * items.
     *
     * Returns `undefined` if the item is stale, unless
     * {@link LRUCache.OptionsBase.allowStale} is set.
     */
    peek(k, peekOptions = {}) {
        const { allowStale = this.allowStale } = peekOptions;
        const index = this.#keyMap.get(k);
        if (index === undefined ||
            (!allowStale && this.#isStale(index))) {
            return;
        }
        const v = this.#valList[index];
        // either stale and allowed, or forcing a refresh of non-stale value
        return this.#isBackgroundFetch(v) ? v.__staleWhileFetching : v;
    }
    #backgroundFetch(k, index, options, context) {
        const v = index === undefined ? undefined : this.#valList[index];
        if (this.#isBackgroundFetch(v)) {
            return v;
        }
        const ac = new AC();
        const { signal } = options;
        // when/if our AC signals, then stop listening to theirs.
        signal?.addEventListener('abort', () => ac.abort(signal.reason), {
            signal: ac.signal,
        });
        const fetchOpts = {
            signal: ac.signal,
            options,
            context,
        };
        const cb = (v, updateCache = false) => {
            const { aborted } = ac.signal;
            const ignoreAbort = options.ignoreFetchAbort && v !== undefined;
            if (options.status) {
                if (aborted && !updateCache) {
                    options.status.fetchAborted = true;
                    options.status.fetchError = ac.signal.reason;
                    if (ignoreAbort)
                        options.status.fetchAbortIgnored = true;
                }
                else {
                    options.status.fetchResolved = true;
                }
            }
            if (aborted && !ignoreAbort && !updateCache) {
                return fetchFail(ac.signal.reason);
            }
            // either we didn't abort, and are still here, or we did, and ignored
            const bf = p;
            if (this.#valList[index] === p) {
                if (v === undefined) {
                    if (bf.__staleWhileFetching) {
                        this.#valList[index] = bf.__staleWhileFetching;
                    }
                    else {
                        this.delete(k);
                    }
                }
                else {
                    if (options.status)
                        options.status.fetchUpdated = true;
                    this.set(k, v, fetchOpts.options);
                }
            }
            return v;
        };
        const eb = (er) => {
            if (options.status) {
                options.status.fetchRejected = true;
                options.status.fetchError = er;
            }
            return fetchFail(er);
        };
        const fetchFail = (er) => {
            const { aborted } = ac.signal;
            const allowStaleAborted = aborted && options.allowStaleOnFetchAbort;
            const allowStale = allowStaleAborted || options.allowStaleOnFetchRejection;
            const noDelete = allowStale || options.noDeleteOnFetchRejection;
            const bf = p;
            if (this.#valList[index] === p) {
                // if we allow stale on fetch rejections, then we need to ensure that
                // the stale value is not removed from the cache when the fetch fails.
                const del = !noDelete || bf.__staleWhileFetching === undefined;
                if (del) {
                    this.delete(k);
                }
                else if (!allowStaleAborted) {
                    // still replace the *promise* with the stale value,
                    // since we are done with the promise at this point.
                    // leave it untouched if we're still waiting for an
                    // aborted background fetch that hasn't yet returned.
                    this.#valList[index] = bf.__staleWhileFetching;
                }
            }
            if (allowStale) {
                if (options.status && bf.__staleWhileFetching !== undefined) {
                    options.status.returnedStale = true;
                }
                return bf.__staleWhileFetching;
            }
            else if (bf.__returned === bf) {
                throw er;
            }
        };
        const pcall = (res, rej) => {
            const fmp = this.#fetchMethod?.(k, v, fetchOpts);
            if (fmp && fmp instanceof Promise) {
                fmp.then(v => res(v === undefined ? undefined : v), rej);
            }
            // ignored, we go until we finish, regardless.
            // defer check until we are actually aborting,
            // so fetchMethod can override.
            ac.signal.addEventListener('abort', () => {
                if (!options.ignoreFetchAbort ||
                    options.allowStaleOnFetchAbort) {
                    res(undefined);
                    // when it eventually resolves, update the cache.
                    if (options.allowStaleOnFetchAbort) {
                        res = v => cb(v, true);
                    }
                }
            });
        };
        if (options.status)
            options.status.fetchDispatched = true;
        const p = new Promise(pcall).then(cb, eb);
        const bf = Object.assign(p, {
            __abortController: ac,
            __staleWhileFetching: v,
            __returned: undefined,
        });
        if (index === undefined) {
            // internal, don't expose status.
            this.set(k, bf, { ...fetchOpts.options, status: undefined });
            index = this.#keyMap.get(k);
        }
        else {
            this.#valList[index] = bf;
        }
        return bf;
    }
    #isBackgroundFetch(p) {
        if (!this.#hasFetchMethod)
            return false;
        const b = p;
        return (!!b &&
            b instanceof Promise &&
            b.hasOwnProperty('__staleWhileFetching') &&
            b.__abortController instanceof AC);
    }
    async fetch(k, fetchOptions = {}) {
        const { 
        // get options
        allowStale = this.allowStale, updateAgeOnGet = this.updateAgeOnGet, noDeleteOnStaleGet = this.noDeleteOnStaleGet, 
        // set options
        ttl = this.ttl, noDisposeOnSet = this.noDisposeOnSet, size = 0, sizeCalculation = this.sizeCalculation, noUpdateTTL = this.noUpdateTTL, 
        // fetch exclusive options
        noDeleteOnFetchRejection = this.noDeleteOnFetchRejection, allowStaleOnFetchRejection = this.allowStaleOnFetchRejection, ignoreFetchAbort = this.ignoreFetchAbort, allowStaleOnFetchAbort = this.allowStaleOnFetchAbort, context, forceRefresh = false, status, signal, } = fetchOptions;
        if (!this.#hasFetchMethod) {
            if (status)
                status.fetch = 'get';
            return this.get(k, {
                allowStale,
                updateAgeOnGet,
                noDeleteOnStaleGet,
                status,
            });
        }
        const options = {
            allowStale,
            updateAgeOnGet,
            noDeleteOnStaleGet,
            ttl,
            noDisposeOnSet,
            size,
            sizeCalculation,
            noUpdateTTL,
            noDeleteOnFetchRejection,
            allowStaleOnFetchRejection,
            allowStaleOnFetchAbort,
            ignoreFetchAbort,
            status,
            signal,
        };
        let index = this.#keyMap.get(k);
        if (index === undefined) {
            if (status)
                status.fetch = 'miss';
            const p = this.#backgroundFetch(k, index, options, context);
            return (p.__returned = p);
        }
        else {
            // in cache, maybe already fetching
            const v = this.#valList[index];
            if (this.#isBackgroundFetch(v)) {
                const stale = allowStale && v.__staleWhileFetching !== undefined;
                if (status) {
                    status.fetch = 'inflight';
                    if (stale)
                        status.returnedStale = true;
                }
                return stale ? v.__staleWhileFetching : (v.__returned = v);
            }
            // if we force a refresh, that means do NOT serve the cached value,
            // unless we are already in the process of refreshing the cache.
            const isStale = this.#isStale(index);
            if (!forceRefresh && !isStale) {
                if (status)
                    status.fetch = 'hit';
                this.#moveToTail(index);
                if (updateAgeOnGet) {
                    this.#updateItemAge(index);
                }
                if (status)
                    this.#statusTTL(status, index);
                return v;
            }
            // ok, it is stale or a forced refresh, and not already fetching.
            // refresh the cache.
            const p = this.#backgroundFetch(k, index, options, context);
            const hasStale = p.__staleWhileFetching !== undefined;
            const staleVal = hasStale && allowStale;
            if (status) {
                status.fetch = isStale ? 'stale' : 'refresh';
                if (staleVal && isStale)
                    status.returnedStale = true;
            }
            return staleVal ? p.__staleWhileFetching : (p.__returned = p);
        }
    }
    /**
     * Return a value from the cache. Will update the recency of the cache
     * entry found.
     *
     * If the key is not found, get() will return `undefined`.
     */
    get(k, getOptions = {}) {
        const { allowStale = this.allowStale, updateAgeOnGet = this.updateAgeOnGet, noDeleteOnStaleGet = this.noDeleteOnStaleGet, status, } = getOptions;
        const index = this.#keyMap.get(k);
        if (index !== undefined) {
            const value = this.#valList[index];
            const fetching = this.#isBackgroundFetch(value);
            if (status)
                this.#statusTTL(status, index);
            if (this.#isStale(index)) {
                if (status)
                    status.get = 'stale';
                // delete only if not an in-flight background fetch
                if (!fetching) {
                    if (!noDeleteOnStaleGet) {
                        this.delete(k);
                    }
                    if (status && allowStale)
                        status.returnedStale = true;
                    return allowStale ? value : undefined;
                }
                else {
                    if (status &&
                        allowStale &&
                        value.__staleWhileFetching !== undefined) {
                        status.returnedStale = true;
                    }
                    return allowStale ? value.__staleWhileFetching : undefined;
                }
            }
            else {
                if (status)
                    status.get = 'hit';
                // if we're currently fetching it, we don't actually have it yet
                // it's not stale, which means this isn't a staleWhileRefetching.
                // If it's not stale, and fetching, AND has a __staleWhileFetching
                // value, then that means the user fetched with {forceRefresh:true},
                // so it's safe to return that value.
                if (fetching) {
                    return value.__staleWhileFetching;
                }
                this.#moveToTail(index);
                if (updateAgeOnGet) {
                    this.#updateItemAge(index);
                }
                return value;
            }
        }
        else if (status) {
            status.get = 'miss';
        }
    }
    #connect(p, n) {
        this.#prev[n] = p;
        this.#next[p] = n;
    }
    #moveToTail(index) {
        // if tail already, nothing to do
        // if head, move head to next[index]
        // else
        //   move next[prev[index]] to next[index] (head has no prev)
        //   move prev[next[index]] to prev[index]
        // prev[index] = tail
        // next[tail] = index
        // tail = index
        if (index !== this.#tail) {
            if (index === this.#head) {
                this.#head = this.#next[index];
            }
            else {
                this.#connect(this.#prev[index], this.#next[index]);
            }
            this.#connect(this.#tail, index);
            this.#tail = index;
        }
    }
    /**
     * Deletes a key out of the cache.
     * Returns true if the key was deleted, false otherwise.
     */
    delete(k) {
        let deleted = false;
        if (this.#size !== 0) {
            const index = this.#keyMap.get(k);
            if (index !== undefined) {
                deleted = true;
                if (this.#size === 1) {
                    this.clear();
                }
                else {
                    this.#removeItemSize(index);
                    const v = this.#valList[index];
                    if (this.#isBackgroundFetch(v)) {
                        v.__abortController.abort(new Error('deleted'));
                    }
                    else if (this.#hasDispose || this.#hasDisposeAfter) {
                        if (this.#hasDispose) {
                            this.#dispose?.(v, k, 'delete');
                        }
                        if (this.#hasDisposeAfter) {
                            this.#disposed?.push([v, k, 'delete']);
                        }
                    }
                    this.#keyMap.delete(k);
                    this.#keyList[index] = undefined;
                    this.#valList[index] = undefined;
                    if (index === this.#tail) {
                        this.#tail = this.#prev[index];
                    }
                    else if (index === this.#head) {
                        this.#head = this.#next[index];
                    }
                    else {
                        const pi = this.#prev[index];
                        this.#next[pi] = this.#next[index];
                        const ni = this.#next[index];
                        this.#prev[ni] = this.#prev[index];
                    }
                    this.#size--;
                    this.#free.push(index);
                }
            }
        }
        if (this.#hasDisposeAfter && this.#disposed?.length) {
            const dt = this.#disposed;
            let task;
            while ((task = dt?.shift())) {
                this.#disposeAfter?.(...task);
            }
        }
        return deleted;
    }
    /**
     * Clear the cache entirely, throwing away all values.
     */
    clear() {
        for (const index of this.#rindexes({ allowStale: true })) {
            const v = this.#valList[index];
            if (this.#isBackgroundFetch(v)) {
                v.__abortController.abort(new Error('deleted'));
            }
            else {
                const k = this.#keyList[index];
                if (this.#hasDispose) {
                    this.#dispose?.(v, k, 'delete');
                }
                if (this.#hasDisposeAfter) {
                    this.#disposed?.push([v, k, 'delete']);
                }
            }
        }
        this.#keyMap.clear();
        this.#valList.fill(undefined);
        this.#keyList.fill(undefined);
        if (this.#ttls && this.#starts) {
            this.#ttls.fill(0);
            this.#starts.fill(0);
        }
        if (this.#sizes) {
            this.#sizes.fill(0);
        }
        this.#head = 0;
        this.#tail = 0;
        this.#free.length = 0;
        this.#calculatedSize = 0;
        this.#size = 0;
        if (this.#hasDisposeAfter && this.#disposed) {
            const dt = this.#disposed;
            let task;
            while ((task = dt?.shift())) {
                this.#disposeAfter?.(...task);
            }
        }
    }
}
exports.LRUCache = LRUCache;
//# sourceMappingURL=index.js.map